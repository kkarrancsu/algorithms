from setuptools import setup

setup(name='summer of algorithms',
      version='0.0.0',
      description='algorithms practice',
      url='',
      packages=['data_structures', 'sorting'],
      zip_safe=False)